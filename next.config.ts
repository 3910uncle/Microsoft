import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // To customize your URL:
  // 1. For Vercel: Deploy and add custom domain in Vercel dashboard
  // 2. For subdirectory (example.com/app): Set basePath below:
  // basePath: '/your-subdirectory',
  
  // For CDN asset optimization (optional):
  // assetPrefix: 'https://cdn.yourdomain.com',
};

export default nextConfig;
